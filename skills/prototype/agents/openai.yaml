interface:
  display_name: "Prototype"
  short_description: "Build a throwaway prototype"
  default_prompt: "Use $prototype to build a throwaway prototype for this design question."

policy:
  allow_implicit_invocation: false
