interface:
  display_name: "To PRD"
  short_description: "Turn the conversation into a PRD"
  default_prompt: "Use $to-prd to turn this conversation into a PRD."

policy:
  allow_implicit_invocation: false
