interface:
  display_name: "To Tickets"
  short_description: "Break a plan into agent-ready tickets"
  default_prompt: "Use $to-tickets to break this plan into independently grabbable tickets."

policy:
  allow_implicit_invocation: false
