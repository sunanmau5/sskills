interface:
  display_name: "To Issues"
  short_description: "Break a plan into agent-ready issues"
  default_prompt: "Use $to-issues to break this plan into independently grabbable issues."

policy:
  allow_implicit_invocation: false
